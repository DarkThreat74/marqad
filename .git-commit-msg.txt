fix: deep audit — race conditions, memory leak, UX edge cases

- Add isStoppingRef guard to prevent double-stop race condition in
  stopRecording, stopBatchRecording, and autoSaveOnFailure
- Clear pendingAudioBlobRef after upload to prevent memory leak
- autoSaveOnFailure now checks isStoppingRef before showing dialog
  (prevents duplicate dialogs if WebSocket drops during manual stop)
- Add beforeunload warning when save dialog is open (prevents data loss)
- Clear period error on successful add (was lingering from previous error)
- Fix CSS z-index conflict (settings overlay now 1001, save dialog 1000)
- Add max-height + overflow-y to save dialog on mobile (keyboard scroll)
- Use ref pattern for processBatchTranscription to avoid forward reference

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
