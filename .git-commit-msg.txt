fix: save dialog on auto-save + more reconnection attempts + time format

- autoSaveOnFailure now shows the save dialog with the proper default name
  instead of silently saving with no title (fixes missing dialog + wrong names)
- Increased WebSocket reconnection attempts from 10 to 20 with capped backoff
  (fixes recording stopping after ~20 minutes due to connection drops)
- Normalize Postgres time format "HH:MM:SS" → "HH:MM" when loading periods
  from DB (fixes default name generation failing on DB-loaded periods)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
